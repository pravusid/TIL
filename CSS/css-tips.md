# CSS tips

## CSS Media Query 반응형 breakpoint

- mobile
- tablet
- laptop
- desktop

## 텍스트 줄바꿈 방지

- `whitespace-nowrap` (css: `white-space: nowrap`): 줄바꿈 방지 + 공백 축약
- `text-nowrap` (css: `text-wrap: nowrap`): 줄바꿈 방지

> text-wrap은 여러가지 줄 바꿈 방식을 설정할 수 있으며 (줄바꿈 방지도 일부), 비교적 최신 브라우저 지원속성임
